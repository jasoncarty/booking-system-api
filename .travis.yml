language: node_js
services: docker
before_install:
  - docker -v
  - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
install:
  - docker build -t jason.carty/booking-system-api .
script:
  - docker-compose up
